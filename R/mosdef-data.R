#' A sample `DESeqResults` object
#'
#' A sample `DESeqResults` object, generated in the `DESeq2` framework
#'
#' @details This `DESeqResults` object is on the data from the `airway` package
#'
#'
#' Details on how this object has been created are included in the `create_mosdef_data`
#' script, included in the `scripts` folder of the `mosdef` package.
#'
#' @references TODO
#'
#' @name res_airway
#' @docType data 
#' 
#' A sample `DESeqDataset` objecz
#' 
#' A sample `DESeqDataset` object, generated in the `DESeq2` framework
#'
#' @details This `DESeqDataset` object is on the data from the `airway` package
#'
#'
#' Details on how this object has been created are included in the `create_mosdef_data`
#' script, included in the `scripts` folder of the `mosdef` package.
#' 
#' @references TODO
#' 
#' @name dds_airway
#' @docType data
#' 
#' A sample enrichment object
#' 
#' A sample enrichment object, generated in the `mosdef` and `topGO` framework
#' 
#' @details This enrichment object is on the data from the `airway` package
#'
#'
#' Details on how this object has been created are included in the `create_mosdef_data`
#' script, included in the `scripts` folder of the `mosdef` package.
#' 
#' @references TODO
#' 
#' @name topgoDE_airway
#' @docType data
#' 
NULL
